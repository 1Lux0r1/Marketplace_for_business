# План разработки дизайна

Что спроектировано, чего нет, и в каком порядке это делается. Сверено
с планами спринтов `docs/05-sprint-0*.md` 6 сентября 2026.

Документ живой: этап закрывается — строка переезжает в «сделано», появилось
расхождение с планом спринтов — дописывается сюда, а не обсуждается заново.

---

## Принцип: дизайн идёт на шаг впереди разработки, а не на три месяца

Макеты в `design/` нарисованы для всего пути клиента сразу — от каталога
до приёмки работы. Это было правильно один раз: путь целиком нужно было
увидеть, чтобы проверить саму идею продукта и показать её.

Дальше так делать не стоит. Оформление заказа и сопровождение сделки
нарисованы за два-три спринта до реализации; за это время изменятся модель
данных, решения по деньгам и по электронной подписи, и макеты придётся
перерисовывать. Рисуем под задачу, которая идёт следующей.

Исключение — когда картинка нужна не для разработки, а для решения:
показать инвестору, проверить путь целиком, договориться о тоне.

---

## Что спроектировано сейчас

| Что | Где | Состояние |
|---|---|---|
| Токены обеих тем, палитра, шкала | `src/app/globals.css`, `design/Tokens.dc.html` | сделано, проверяется тестами |
| Четыре состояния экрана | `src/ui/states.tsx`, `design/States.dc.html` | сделано |
| Шрифт Manrope | `src/ui/fonts/` | сделано |
| Каталог, карточка услуги | `design/Main.dc.html`, `design/Listing.dc.html` | сделано, витрина стоит на главной |
| Оформление заказа | `design/Checkout.dc.html` | макет есть, нужен в спринте 04 |
| Сопровождение сделки | `design/Deal.dc.html` | макет есть, нужен в спринте 05 |
| Каркас ролей: меню и шапка экрана | `src/app/sections.ts`, `src/ui/page-header.tsx` | сделано |

**Примитивы дизайн-системы:** кнопка, поле ввода, многострочное поле,
выпадающий список, поле цены, галочка, множественный выбор, бейдж статуса,
тег выгоды, таблица, строка списка, вкладки, чипы фильтров, разбивка списка,
шторка, окно подтверждения, четыре состояния, шапка экрана.

Все они живьём на `/ui-kit` — экран, где примитивы лежат рядом и их можно
потрогать. Компонент проверяется не по коду, а глазами и с клавиатуры,
и такой экран находит расхождения раньше, чем они расползутся по настоящим
экранам. В меню его нет: это инструмент разработки, а не раздел продукта.

---

## Чего нет

Разрыв между тем, что нарисовано, и тем, что нужно ближайшими задачами:

| Экран | Нужен по плану | Состояние |
|---|---|---|
| Экраны оператора: подрядчики, категории, зоны | 01-4 | собран, не оформлен |
| Кабинет подрядчика: свои карточки услуг | 01-5 | нет |
| Очередь модерации карточек | 01-5 | нет |
| Очередь регистраций подрядчиков | 02-2 | собран, не оформлен |
| Кабинет клиента: компания и объекты | 02-3 | собран, не оформлен |
| Регистрация подрядчика, выбор пароля | 02-2, 02-3 | собран, не оформлен |
| Весь интерфейс администратора | спринт 03 | нет |

«Собран, не оформлен» — экран работает и живёт по своему адресу, но сделан
из голой разметки, а не из примитивов дизайн-системы: отступы и цвета набраны
по месту, состояний §7.4 у него нет или они неполные. Это очередь этапа 5.

---

## Этапы

**Этап 1. Каркас ролей — сделано.** Меню собирается из ролей компании,
а не зашито; шапка экрана с одним главным действием и путём до него.
Правило §7.1 («разделы называются задачами человека») проверяется тестом
`src/app/sections.test.ts`, включая перечень слов, которых роль не должна
видеть никогда.

**Этап 2. Примитивы рабочих экранов — сделано.** Выпадающий список,
многострочное поле, поле цены, множественный выбор, вкладки, чипы фильтров,
строка списка, разбивка списка, окно подтверждения. Нужны сразу в 01-4,
01-5 и 02-2, поэтому сделаны один раз и до них.

**Этап 3. Экраны 01-4 и 01-5.** Кабинет подрядчика и два экрана оператора.
Проектируются и собираются вместе с задачей.

**Этап 4. Витрина, 02-1 — сделано.** Экраны собрал второй агент без оформления,
одеты по макетам: отраслевая иллюстрация на карточке, крупная цена, отметка
проверенного ИНН и оценка рядом с подрядчиком, плашка гаранта над витриной,
«Как защищена сделка» шагами на карточке услуги.

Чего из макета нет и почему: бейджа скидки и числа закрытых заказов — таких
полей в данных не существует, а выдумывать их нельзя (§9.7). Места под них
оставлены.

**Этап 5. Одеть собранные экраны.** Кабинет клиента, очередь проверки,
подрядчики у оператора, регистрация подрядчика и выбор пароля. Все они уже
работают — задача не в том, чтобы их сделать, а в том, чтобы перевести их
на примитивы этапа 2 и дописать состояния §7.4.

**Этап 6. Интерфейс администратора, спринт 03.** Отдельный интерфейс со своей
плотностью и своими задачами (`docs/05-sprint-03.md` объясняет, чем он
отличается от интерфейса оператора). §7.1 пунктов меню для администратора
не задаёт — это Q21 в `docs/06-open-questions.md`.

---

## Решения, принятые по ходу

**Меню одно на всё, без переключателя режима** (решение разработчика
от 06.09.2026). `docs/12-auth-ux.md` предлагал переключатель «Я заказываю /
Я выполняю»; выбран другой вариант — все разделы сразу.

Цена решения названа и оказалась меньше ожидаемой: у компании, которая
и заказывает, и выполняет, девять пунктов, и на широком экране они помещаются.
На узком — нет, поэтому в меню сделаны две вещи, без которых решение работало
бы плохо:

- **группы разделены чертой** — иначе «Компания» и «Мои услуги» читаются
  как соседи по смыслу, хотя это разные половины жизни компании на площадке;
- **активный пункт доводится до видимой части** — §7.1 требует, чтобы
  с любого экрана было видно, где ты находишься, а в прокрученной строке
  активный пункт иначе уезжает за край.

**Поведение модального окна — одно на всех.** Ловушка фокуса, `Esc`
и возврат фокуса живут в `use-dialog.ts` и используются и шторкой,
и окном подтверждения. Две копии — ровно то место, где доступность
разъезжается: одну поправят, вторую забудут.

**Примитив сам разбирается со своими требованиями.** Вкладки и разбивка
списка читают адрес, а Next разрешает это только внутри границы ожидания.
Граница стоит внутри примитива, а не у того, кто ставит его на экран:
компонент, который роняет сборку, если о нём не вспомнить, будет ронять
её регулярно.

**Суммы и статусы выровнены полосами.** У статуса, суммы и действий свои
полосы одинаковой ширины: иначе строка с одной кнопкой сдвигает свою сумму
относительно строки с двумя, и колонка сумм перестаёт читаться сверху вниз
вопреки §7.3.

**Категории различает рисунок, а не цвет.** Первый вариант раскрашивал
их по смыслу: электрику оранжевым, пожарную безопасность красным, охрану
труда зелёным. Это нарушение §7.2 — статусный цвет не используется как
украшение: рядом на витрине стоят настоящие бейджи этих же цветов, и один
цвет с двумя смыслами не читается. Подложка у всех одна, цветными остаются
только действие, выгода и состояние.

**Витрина — это главная.** §7.1 требует, чтобы главная отвечала на вопрос
«что мне делать сейчас», а каталог — основной путь клиента (§1). Отдельный
начальный экран перед каталогом отвечал бы на этот вопрос хуже, чем сам
каталог, поэтому его нет. Технически витрина лежит в папке `(storefront)`
со скобками: скобки убирают папку из адреса, но оставляют рядом с ней
состояния загрузки и ошибки — без этого они стали бы общими для всех
экранов, и на кабинете клиента показывался бы скелет каталога.

**Незашедший видит только витрину.** Раздел, который отвечает «сначала
войдите», — это тупик, а не приглашение: у человека без входа нет компании,
и показывать ему «Документы и счета» не за чем.

**Коды не показываются человеку.** Округ хранится кодом `msk-cao` — один
источник на всю систему, иначе в базе окажутся «МСК-ЦАО» и «центр»
вперемешку. Но на экран он выходит названием: перевод живёт в
`src/server/zone-names.ts` и проверяется тестом, потому что один раз код
уже протёк на карточку услуги.

**Данные меню отделены от разметки.** `sections.ts` знает названия, адреса
и роли, но не знает про React: иконка хранится именем из реестра. Поэтому
главное правило информационной архитектуры проверяется тестом без браузера
и без базы.
